# Generated by the lex toolchain (ACLI scaffold) — regenerate, never commit.
.cli/

# Per-project agent state the tool writes as it runs: todos, plans, session
# trails, and the project-memory store. Derived, machine-local, never source.
.lex/
